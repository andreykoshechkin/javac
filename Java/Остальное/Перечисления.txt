enum				//Список который состоит их опеределенных перечислений.			
				//enum может иметь приватный конструктор.
				//Объекты enum можно сравнивать через ==
				
Метод:				//values - переводит наше перечисление в массив.
				  ИмяПеречисления [] massive =  ИмяПеречисления.values();