Daemon thread: 				//Это потоки которые выполняют какие-то задачи в фоновом режиме к прмеру: garberCollection, выполняется в фоновом режиме
					//setDeamon(true) - создание daemon потока, в параметрах нужно указать true, так мы скажем, что поток является daemon.
					  Примечание: Метод setDeamon() нужно вызывать после создания потока и перед запуском потока.
					
					//isDeamon() - проверяет, является ли потоко демоном 