в Html странцу нужно добавить:
<div>
        <img th:src="@{/api/v1/users/{userId}/avatar(userId=${user.id})}"> <!--именно тег <img> будет отправляться запросом в бд на получения картинки-->
</div>
======================================================================================================================