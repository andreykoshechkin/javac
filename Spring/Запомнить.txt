NativeQuery лучше не использовать, так как использование нативных запросов(запросов напрямую к бд) плохо сказывается на производительности, так как
мы не можем использовать entityGrapth, реализовывать lazy загрузку с использванием fetch(fetch join)