использовать final поля позволит класс сделать неизменяемым.

public class Example {
private final Repository repository 	- теперь Repository неизменяемый
}


===============================

Класс не должен отвечать за создание объекта, поэтому, в этом классе, инициализация полей происходит через конструктор