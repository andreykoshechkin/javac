batchUpdate()	<-- для обновление данных батчем
call()		<-- Для работы с хранимыми процедурами(на практике не так часто встречается)
execute()	<-- Для выполнения любый запросов, в том числе и DDL (Не рекомендован)
query()		<-- Для выполнения selectзапросов (Рекомендован)
queryForList()  <-- Для выполнения select запросов, когда мы хотим сразу вернуть список (Используется для возвращения одной колонки, пример: список ID) Вернуть DTO мы не можем.
		    заисключением того queryForList(), который возвращает Map.
queryForMap()	<-- Выполняет select запрос и возвращает Map<>
update()	<-- Для выполнения update,delete,insert операций


