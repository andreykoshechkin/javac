UNION 		//Возвращает из двух множест только уникальные элементы, как set в java 
UNION ALL 	//Возвращает все элементы, в том числе и те который дублируются,  из таб1 и таб2
INTERSECT 	//Возращает те элементы, который пересикаются(Вернет только те элементы, которые есть и в 1таб и во 2ой таб ) ПЕРЕСЕЧЕНИЕ
except		//Вернет только те элементы  которые есть в 1таб с условием, что во 2ойТаб этих элементов нет